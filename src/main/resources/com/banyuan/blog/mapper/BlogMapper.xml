<?xml version="1.0" encoding="UTF-8" ?>
        <!DOCTYPE mapper
                PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
                "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

        <!-- 命名空间namespace:指定接口全类名 -->
<mapper namespace="com.banyuan.blog.mapper.BlogMapper">
<select id="selectBlogById" resultType="Blog">
    SELECT id, title, user_id, content, time, comment_num
    FROM blog
    WHERE id = #{id}
</select>

<select id="selectBlogByUsername" resultType="Blog">
    SELECT blog.id, title, user_id, content, time, comment_num
    FROM blog
             LEFT JOIN
         user ON blog.user_id = user.id
    WHERE user.username = #{username}
</select>
</mapper>
